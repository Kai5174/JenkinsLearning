pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'python --version'
                sh 'ls'
                sh 'sudo apt-get install vim'
            }
        }
    }
}
