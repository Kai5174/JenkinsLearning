pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'python --version'
                sh 'ls'
                sh 'apt-get install vim'
            }
        }
    }
}
